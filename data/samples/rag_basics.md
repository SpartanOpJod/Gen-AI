# Retrieval Augmented Generation

Retrieval-augmented generation combines a retriever with a language model so answers are grounded in retrieved context.

Chunking splits long documents into smaller passages, and overlap preserves surrounding context between chunks.

The project uses sentence-transformers embeddings and a vector database for similarity search.
